ENV['MATCH_GIT_BASIC_AUTH_USERNAME']=ENV['GITLAB_USERNAME']
ENV['MATCH_GIT_BASIC_AUTH_PASSWORD']=ENV['GITLAB_PASSWORD']

git_url("https://gitlab.com/primer-io/acceptance/mobile/ios-sdk-match-repo.git")
# git_branch("nq/update-ios-certificates") # When updating, we must do it on a branch

storage_mode("git")

type("development") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier("com.primerapi.PrimerSDKExample")
username("security@primerapi.com") # Your Apple Developer Portal username
# api_key_path("./JM91JR5L1QGH.json")

platform("ios")
shallow_clone true
force_for_new_devices true
clone_branch_directly true

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match
